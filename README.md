# JoinTeleport

Join(サーバーに参加)時に指定した場所にテレポートします


コマンド：
op専用
/jotp on システムを有効かさせます(必ず/jotp setをセットしてから実行してください)
/jotp off システムを無効化させます

/jotp opon opもJoin時にテレポートさせます(システムを有効にしていないと意味がありません)
/jotp opoff opをJoin時にテレポートさせなくします

/jotp set テレポート地点を設定します


改造はokですが
二次配布は禁止です
